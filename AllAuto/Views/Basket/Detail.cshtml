@model IEnumerable<AllAuto.Domain.ViewModels.Order.ItemEntryViewModel>

@{
    ViewBag.Title = "Корзина";
    Layout = "_Layout";
}

@if (Model == null || Model.Count() == 0)
{
    <div style="padding:40px;"></div>
    <div class="card col-md-12">
        <div class="row g-0">
            <div class="col-md-12">
                <div class="card-body">
                    <h5 class="card-title text-center">Список заказов пуст</h5>
                </div>
            </div>
        </div>
    </div>
}
else
{
    <div class="container ">
        <div class="row justify-content-center">
            <table class="table w-75">
                <thead>
                    <tr>
                        <th scope="col">Наименование</th>
                        <th scope="col">Производитель</th>
                        <th scope="col">Цена</th>
                        <th scope="col">Количество</th>
                        <th scope="col">Удалить</th>
                    </tr>
                </thead>

                @foreach (var item in Model)
                {
                    <tbody>
                        <tr>
                            <th scope="row"> @item.Name</th>
                            <td>@item.Manufactor</td>
                            <td>@item.Sum ₽</td>
                            <td>@item.Quantity</td>
                            <td><a asp-controller="Order" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Удалить</a></td>
                        </tr>
                    </tbody>
                }

                <tbody>
                    <tr>
                        <th scope="row">Итого</th>
                        <td></td>
                        <td>@Model.Sum(x => x.Sum) ₽</td>
                        <td></td>
                    </tr>
                </tbody>
            </table>

            <div class="row col-4 justify-content-center">
                <a asp-controller="Basket" asp-action="CreateOrder" class="btn btn-primary">Оформить</a>
            </div>
        </div>
       
    </div>
   

    
}